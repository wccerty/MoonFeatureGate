///|
/// MoonFeatureGate provides local feature flag evaluation for MoonBit projects.
///
/// The public API is built up in focused files so reviewers can inspect
/// rule matching, rollout bucketing, providers, and parsing independently.
